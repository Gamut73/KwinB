plugins {
    kotlin("jvm") version "2.3.21"
    application
}

group = "{# @pebvariable name="groupId" type="string" #}{{groupId}}"
version = "1.0-SNAPSHOT"

application {
    mainClass.set("Main.kt")
}

repositories {
    mavenCentral()
}

dependencies {
    testImplementation(kotlin("test"))
}

kotlin {
    jvmToolchain(21)
}

tasks.test {
    useJUnitPlatform()
}